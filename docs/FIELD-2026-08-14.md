# Field session 2026-08-14 — Nevada City: first real control + fleet OTA

First on-site session. Full path proven on real hardware: **browser (Elliot's
merged app) → cambium daemon → CoreS3 bridge → ESP-NOW → lanterns**, including
an over-the-air reflash of ten fixtures, all from one laptop at the tree.

## What happened, in order

1. **Bridge**: the on-site CoreS3 (mac `80456B4D5DB0`, id `4D5DB0`) arrived
   running the audio-reactive artifact. Rebuilt and flashed Ben's same
   firmware with `--cambium` (identity `cores3-cb-0.1`), channel 11.
   `cambium doctor` then heard 16 lanterns; zero CRC errors all day.
2. **Roll-call + identify**: `cambium blink` and broadcast identify worked on
   every reachable lantern regardless of firmware age (identify is original
   protocol). Fleet-wide blinking green confirmed downlink RF reaches the tree.
3. **Streaming initially failed**: zero lanterns held a direct-frame color.
   Deployed firmware (`fixture-2026-08-10.x`) predates nothing — but see 4;
   the effective cause was that direct frames only landed intermittently
   ("brief blips"), while long-TTL identify masked the loss.
4. **Fleet OTA over the bridge** (ADR-0010 flow, all radio + site WiFi):
   - broadcast `NB_ENTER_MAINT` via `tools/send_enter_maint.py`
   - ten lanterns joined the site WiFi (low batteries passed on the solar
     override; sun did the work)
   - flashed all ten **in parallel** via `POST /update`, ~2 minutes total:
     `F2B7DC F2BDB0 F2BE0C F2BE20 F2BE48 F2BEA4 F2BEE4 F2BF8C F40268 F40364`
   - artifact: `fixture/build/onsite-fixture-r2/fixture.ino.bin`, built from
     beneckart/resonance-lighting main @ `d52d249`, channel 11, profile prod,
     site-WiFi credentials baked (gitignored `wifi_secrets.h`), sha256
     `ffa5ebd3fe392852…` (full hash in build dir)
   - every unit validated, rebooted, rejoined the mesh
5. **After OTA: 8/8 rostered lanterns held solid green** via direct frames
   with a 1 Hz refresh. Night override (`NB_FORCE_LIFECYCLE`) obeyed.
   Fleet released to `auto` lifecycle at session end.

## Notes for Ben

- The CoreS3 now runs the **cambium modem build**; restore the audio artifact
  any time with `cores3_bridge/build.sh` without `--cambium`.
- Ten units above now run a binary built from your exact main (`d52d249`) —
  same code, new bake: **site-WiFi credentials + channel 11 + prod**. Version
  string is unchanged from source, so registry-wise treat these as
  re-flashed `2026-08-14` (artifact sha in the build dir on Justin's laptop).
- The **9E/9F-series units never joined WiFi** — consistent with your log:
  they carry the retired WonkyHouse bake and need the one-time USB credential
  pass (your 15/24 list). They identify/blink fine meanwhile.
- Direct-frame delivery before the reflash looked *very* lossy (fixtures
  reported `dl_pdr=0`), worth a look at your new receive counters during the
  next session; after the reflash, 1 Hz refresh held color rock-solid.
- `tools/send_enter_maint.py` in this repo sends types 3/4/16 through the
  cambium bridge (daemon stopped). Batch OTA = broadcast + ARP-scan for
  `68:ee:8f` + parallel `curl -F "file=@…" http://<ip>/update`.

## Notes for Elliot

- **Your merged app drove the real tree today** — the CambiumBridge transport
  + 📡 drive real worked against the live daemon unchanged. BLACKOUT reaches
  hardware.
- Patterns look spatially scrambled on the tree: the 18-lantern roster
  (`config/roster-onsite.csv`) maps your fixture ids to radio MACs
  arbitrarily because nothing is position-mapped yet. The Constellate sweep
  is the next session's job; after it, `?fixtures=` gets the measured file
  and patterns will land where they belong.
- Lanterns dim relative to the sim = each fixture's power budget clamping on
  low batteries (doctrine, not a bug).

## Addendum: second bridge

A second CoreS3 (id `E39F1C`, mac `441BF6E39F1C`) was flashed with the same
cambium modem artifact later in the session and verified by doctor (STATUS +
full census). Both CoreS3 units now carry the modem build; either serves as
the tree bridge, the other is a spare (or reverts to Ben's dashboard/audio
artifact via `cores3_bridge/build.sh` without `--cambium`).

## Current state / next session

- Streamable now: 10 lanterns (fresh firmware). Identify/sweep-able: all.
- Next: Constellate sweep (positions), USB credential pass on the 9E/9F
  units, and re-run the rate math with real `dl_pdr` counters at night.
